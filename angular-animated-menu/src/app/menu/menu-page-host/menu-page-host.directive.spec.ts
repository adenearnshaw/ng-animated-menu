import { MenuPageHostDirective } from './menu-page-host.directive';

describe('MenuPageHostDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuPageHostDirective(null);
    expect(directive).toBeTruthy();
  });
});
